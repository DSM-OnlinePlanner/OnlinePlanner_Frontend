import styled, { css } from "styled-components";
const medium = 500;
const regular = 400;

export const SectionTitle = styled.div`
    font-size: 24px;
    font-weight: ${medium};
    margin-bottom: 10px;
`;